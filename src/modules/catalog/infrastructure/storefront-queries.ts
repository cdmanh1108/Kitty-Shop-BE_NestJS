/**
 * Storefront queries barrel export.
 * Preserves backwards compatibility while responsibilities are separated into:
 * - storefront-product.queries.ts
 * - storefront-category.queries.ts
 */
export * from './storefront-product.queries';
export * from './storefront-category.queries';
